{
  "name": "m-customs",
  "version": "1.0.0",
  "description": "نظام التخليص الجمركي — Al-Sudais Logistics",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^10.7.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
